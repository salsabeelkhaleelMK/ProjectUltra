# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

# Environment variables
.env
.env.local

# npmrc tokens (but keep the template .npmrc)
.npmrc

node_modules
# dist  # Commented out - commit pre-built files to avoid build minutes
dist-ssr
*.local

# Local development files (restored from _package.json for npm)
# These are generated locally but _package.json is what's committed for Netlify
package.json
package-lock.json

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?
